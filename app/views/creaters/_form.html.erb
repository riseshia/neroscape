<%= form_for(@creater) do |f| %>
  <% if @creater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creater.errors.count, "error") %> prohibited this creater from being saved:</h2>

      <ul>
      <% @creater.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :furigana %><br>
    <%= f.text_field :furigana %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
