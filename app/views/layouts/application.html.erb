<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <title id="title">Eroscape</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0,user-scalable=no">
  <meta name="description" content="">
  <meta name="author" content="">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#div-nav-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Eroscape", "/", class: "navbar-brand" %>
      <%= form_tag("/result", method:"GET", role: "form", class: "navbar-form navbar-right") do %>
        <div class="form-group">
          <%= text_field_tag :keyword, params[:keyword], placeholder: "Search", class: "form-control" %>
        </div>
        <%= submit_tag "Search", class: "btn btn-default" %>
      <% end %>
    </div>
  </nav>
<div class="wrapper">
  <div class="col-md-2 my-page">
<% if user_signed_in? %>
    <div class="panel panel-default">
      <div class="panel-heading">
        <div class="panel-title">
          <%= current_user.name %> 님, 안녕하세요?
        </div>
      </div>
      <div class="panel-body">
  <li><%= link_to '나의 통계', "/user_stats?id=#{current_user().id}", :class => 'navbar-link' %></li>
  <br>
  <li><%= link_to '나의 리뷰', "/user?id=#{current_user().id}", :class => 'navbar-link' %></li>
  <li><%= link_to '모든 리뷰', reviews_path, :class => 'navbar-link' %></li>
  <br>
  <li><%= link_to '츠미게', "/tsumige?id=#{current_user().id}", :class => 'navbar-link' %></li>
  <br>
  <li><%= link_to '게임 목록', games_path, :class => 'navbar-link' %></li>
  <li><%= link_to '회사 목록', brands_path, :class => 'navbar-link' %></li>
  <br>
  <li><%= link_to "나가기", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></li>
      </div>
    </div>
<% end %>
  </div>
  <div class="col-md-10">
<% if notice %>
<p class="notice"><%= notice %></p>
<% end %>

<% if alert %>
<p class="alert"><%= alert %></p>
<% end %>
<%= yield %>
  </div>
</div>
</body>
</html>
